import 'package:flutter/material.dart';

class CloudTeleScreen extends StatelessWidget {
  const CloudTeleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cloud TELE'),
      ),
    );
  }
}
