import 'package:flutter/material.dart';

class CloudCareScreen extends StatelessWidget {
  const CloudCareScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cloud CARE'),
      ),
    );
  }
}
