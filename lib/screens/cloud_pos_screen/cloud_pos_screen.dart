import 'package:flutter/material.dart';

class CloudPosScreen extends StatelessWidget {
  const CloudPosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cloud POS'),
      ),
    );
  }
}
